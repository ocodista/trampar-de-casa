describe('Email Pre Renderer', () => {
  it.todo('Access the id of each subscriber')
  it.only('Save on queue {userEmail: {roles, footerHTML, headerHTML}}', async () => {
    await emailPreRenderer()
  })
})
